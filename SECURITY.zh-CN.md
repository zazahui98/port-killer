# 安全策略

[**English**](./SECURITY.md) · **简体中文**

## 这个程序会做什么

Port Killer 会**终结你指定的进程**。这是它的核心功能，也是它最大的风险面。
在报告安全问题之前，请先了解它的设计边界：

- 所有端口查询与进程终结都在本地完成，**不联网、不上报、不收集任何数据**
- 不产生子进程（macOS 上会直接执行系统自带的 `lsof` 用于读取端口信息）
- 不拼接命令行字符串，用户输入不会被送进任何 shell
- 端口号（1–65535）与 PID 在前端和 Rust 两侧各自校验一遍
- 默认走普通终结；「强制终结」只在普通终结失败后出现，且需要独立的二次确认
- 终结后会重新查询端口，只有确实释放了才报告成功

## 支持范围

| 版本 | 是否接受安全修复 |
| --- | --- |
| 最新 release | ✅ |
| 更早版本 | ❌ 请先升级 |

## 报告漏洞

**请不要为安全漏洞开公开 Issue。**

优先使用 GitHub 的 [Private vulnerability reporting](https://docs.github.com/en/code-security/security-advisories/guidance-on-reporting-and-writing-information-about-vulnerabilities/privately-reporting-a-security-vulnerability)
（仓库 Security 标签页 → Report a vulnerability）。

如果不方便使用，也可以发邮件给仓库维护者（见仓库主页联系方式），标题注明 `[SECURITY]`。

请尽量包含：

- 受影响版本与平台
- 漏洞类型（例如权限提升、命令注入、越界读、IPC 输入未校验）
- 复现步骤或最小复现样例
- 影响评估：攻击者能借此做什么
- 如果有，附上修复建议

## 我们关心的漏洞类型

按优先级从高到低：

1. **IPC 层输入校验绕过** —— 绕过端口 / PID 校验导致越界或异常行为
2. **命令注入** —— 任何能把用户输入送进 shell 的路径（当前设计上不存在，若发现请务必报告）
3. **越界读写** —— 原生层解析系统返回的二进制缓冲区时的内存安全问题
4. **权限提升** —— 以普通用户身份终结了本不该能终结的进程
5. **数据外泄** —— 任何形式的联网或上报

## 响应时间

维护者会尽力在 **7 天内**确认收到，并在确认后 **30 天内**给出修复或处理计划。
这是尽力而为的承诺，不是 SLA。

## 已知的非漏洞

以下行为是**设计如此**，不属于安全问题：

- **普通权限下无法终结其他用户 / 系统进程** —— 这是操作系统的权限模型，不是缺陷。
  程序会返回结构化的「权限不足」错误并引导用户以管理员 / root 身份重试。
- **终结进程可能导致依赖它的程序异常或丢失未保存数据** —— 这是终结进程的固有后果。
  界面在确认弹窗中明确提示，强制终结另有二次确认。
- **`lsof` 在 macOS 上不可用** —— 会返回明确的错误，不会静默失败。
